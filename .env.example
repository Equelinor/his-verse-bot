# his.verse.for.the.day — Environment Variables
# Copy to .env for local testing. Never commit .env to GitHub.
# Add these as Secrets in GitHub → Settings → Secrets and variables → Actions

UNSPLASH_ACCESS_KEY=your_unsplash_access_key
OPENAI_API_KEY=your_openai_api_key
PAT_TOKEN=your_github_personal_access_token_ghp_xxx
MAKE_WEBHOOK_URL=https://hook.eu1.make.com/your_webhook_id
